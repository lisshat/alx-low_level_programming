#include "main.h"
#include <string.h>

/**
 *_strlen - function to get length of a string
 *@s: pointer string given to this function
 *Return - returns length of a string 
 */
int _strlen(char *s)
{
	return (_strlen(s));
}
